export interface IContact {
    name: string;
    email: string;
    phone: string;
    id: string;
}

export interface IContactUpdate {
    name: string;
    email: string;
    phone: string;
    id: string;
}