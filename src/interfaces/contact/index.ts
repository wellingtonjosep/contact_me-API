export interface IContact {
    name: string;
    email: string;
    phone: number;
    id: string;
}

export interface IContactUpdate {
    name: string;
    email: string;
    phone: number;
    id: string;
}